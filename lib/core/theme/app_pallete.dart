import 'package:flutter/material.dart';

class AppPallete {
  // Soft, warm cream background to reduce eye strain compared to pure white
  static const Color backgroundColor = Color.fromRGBO(253, 248, 243, 1);

  // Warm Sunset/Peach Gradient Spectrum
  static const Color gradient1 = Color.fromRGBO(255, 126, 95, 1); // Coral Pink
  static const Color gradient2 = Color.fromRGBO(254, 180, 123, 1); // Soft Peach
  static const Color gradient3 = Color.fromRGBO(255, 209, 102, 1); // Warm Amber

  // Soft warm border for UI elements
  static const Color borderColor = Color.fromRGBO(230, 218, 206, 1);

  // Primary text/icon color (deep warm dark brown instead of harsh black)
  static const Color textColor = Color.fromRGBO(44, 34, 30, 1);

  // Muted neutral for secondary text/subtitles
  static const Color greyColor = Color.fromRGBO(140, 126, 120, 1);

  // High contrast error color
  static const Color errorColor = Color.fromRGBO(217, 83, 79, 1);

  // Utility colors
  static const Color whiteColor = Colors.white;

  // Transparent
  static const Color transparentColor = Colors.transparent;
}
